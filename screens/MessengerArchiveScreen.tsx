import React, { useEffect, useState } from "react";
import { ActivityIndicator, FlatList, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { Ionicons } from "@expo/vector-icons"; import { SafeAreaView } from "react-native-safe-area-context"; import { useNavigation } from "@react-navigation/native"; import type { NativeStackNavigationProp } from "@react-navigation/native-stack";
import { listConversations, type MessengerConversation } from "../src/services/messenger"; import { colors, spacing, typography } from "../src/theme"; import type { RootStackParamList } from "../src/navigation/types"; import { ConversationRow } from "./MessengerListScreen";
type Nav = NativeStackNavigationProp<RootStackParamList>;
export default function MessengerArchiveScreen() { const navigation = useNavigation<Nav>(); const [items, setItems] = useState<MessengerConversation[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { listConversations({ archived: true }).then(setItems).finally(() => setLoading(false)); }, []); return <SafeAreaView style={styles.root}><View style={styles.header}><TouchableOpacity style={styles.icon} onPress={() => navigation.goBack()}><Ionicons name="chevron-back" size={27} color={colors.textPrimary} /></TouchableOpacity><Text style={styles.title}>Архив</Text><View style={styles.icon} /></View>{loading ? <ActivityIndicator style={styles.loader} /> : <FlatList data={items} keyExtractor={(item) => item.id} contentContainerStyle={styles.list} renderItem={({ item }) => <ConversationRow item={item} onPress={() => navigation.navigate("MessengerChat", { conversationId: item.id })} />} ListEmptyComponent={<View style={styles.empty}><Ionicons name="archive-outline" size={44} color={colors.textTertiary} /><Text style={styles.emptyTitle}>Архив пуст</Text><Text style={styles.emptyText}>Архивированные диалоги будут храниться здесь.</Text></View>} />}</SafeAreaView>; }
const styles = StyleSheet.create({ root: { flex: 1, backgroundColor: colors.white }, header: { height: 58, flexDirection: "row", alignItems: "center", justifyContent: "space-between", paddingHorizontal: spacing.md }, icon: { width: 42, height: 42, alignItems: "center", justifyContent: "center" }, title: { ...typography.section }, loader: { marginTop: 80 }, list: { paddingHorizontal: spacing.lg, flexGrow: 1 }, empty: { alignItems: "center", marginTop: 100 }, emptyTitle: { ...typography.section, marginTop: spacing.md }, emptyText: { ...typography.secondary, color: colors.textSecondary, marginTop: spacing.xs } });
